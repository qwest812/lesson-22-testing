QUnit.test("equal",function (assert) {
    // expect(3);
    let one =1, two=2, three=3;
    assert.equal(one, 1, "1 equal one");
    assert.equal(two, 1, "1 equal one");
    assert.equal(three, 3, "1 equal one");

});